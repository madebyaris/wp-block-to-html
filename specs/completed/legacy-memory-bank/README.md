# Legacy Memory Bank Snapshot

This directory preserves the full historical contents that previously lived in `memory-bank/`.

It was migrated into `specs/` so the old workflow directories could be removed without losing project history.
